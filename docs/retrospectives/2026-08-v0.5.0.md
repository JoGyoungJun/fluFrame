# Retro — v0.5.0 "--backend firebase" (compact)

- **Milestone**: v0.5.0 (closed 2026-08-03) — issue #40, PRs #41 #42
- **Release**: [fluframe 0.5.0](https://pub.dev/packages/fluframe) —
  second fully automated publish; pipeline now boring (the goal).

## Highlights

- ADR 0001 mechanism reused with zero changes — stage 3 took a fraction
  of stage 2's effort, validating the abstraction.
- Firebase's non-automatable step handled honestly: compile-safe
  `DefaultFirebaseOptions` stub that throws with the exact fix command.
- e2e now covers 3 variants (base / supabase / firebase), all green.

## Action items

1. Announce (final carry — due 08-10): headline is now "auth + your
   choice of Supabase/Firebase in one command".
2. v0.6.0: `fluframe doctor` per the approved roadmap.
