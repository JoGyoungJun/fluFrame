# Retro — v0.2.0 "Polish"

- **Milestone**: v0.2.0 (closed 2026-08-03)
- **Cycle**: second `/cycle` run — first run in **usage-gated loop mode**
  (weekly-limit gate checked before every issue; started at 89% weekly)
- **Release**: [fluframe 0.2.0](https://pub.dev/packages/fluframe),
  published 2026-08-03, tag `fluframe-v0.2.0`

## Metrics

| Metric | Value |
|---|---|
| Issues planned / completed | 4 / 4 (100%) — #12 #15 #16 #17 |
| Carried over / added mid / dropped | 0 / 0 / 0 |
| PRs merged in milestone | 4 (#18–#21 incl. release), all green first run |
| Work-loop failures / skips / gate stops | 0 / 0 / 0 (weekly stayed 89%, gate 96% never hit) |
| CI pass rate on main (all-time, 15 runs) | 100% |
| External contributors | 0 |
| **pub.dev score (v0.1.0, pana)** | **160/160 — perfect** |
| Downloads (30d) | not yet aggregated |
| TODO/FIXME/HACK count | 0 (unchanged from v0.1.0 baseline) |

## What went well

- **Perfect pub score confirmed** (160/160) — the v0.1.0 metadata work
  (topics, example, description window) left nothing on the table.
- **Usage-gated looping worked**: checking the weekly limit before each
  issue let the cycle run safely at 89% utilization; small-issue scoping
  kept consumption low enough that the 96% stop-gate never fired.
- Every CI annoyance and CLI rough edge targeted this milestone got
  fixed: zero Node deprecation warnings, friendly errors for the two
  most likely first-run failures (bad `--org`, missing Flutter).

## What went poorly

- Nothing failed this cycle. Watch item: the loop's per-issue overhead
  (branch + PR + CI wait ≈ 2min) is fine at this scale but will add up
  if a future milestone has many XS issues — batching tiny related
  changes into one issue is allowed and should be preferred.

## Previous action items (from 2026-08-v0.1.0)

1. Fix `/release` publish TTY note — **DONE** (in the v0.1.0 retro PR;
   `--force` path used successfully this release).
2. Announce v0.1.0 — **NOT DONE** (deadline 2026-08-10 still open; now
   announce v0.2.0 instead — owner: maintainer).
3. Check pub points after pana — **DONE** (#12: 160/160, no losses).
4. Windows Developer Mode decision — **NOT DONE** (#13 open, no
   deadline pressure; carry).

## Action items (max 5)

1. **Announce v0.2.0** (carries item 2) — owner: maintainer, by
   2026-08-10. The score is 160/160 and the CLI is polished; adoption is
   now purely a visibility problem.
2. **Decide #13 (Windows Developer Mode)** — owner: maintainer, by next
   release. Second consecutive carry → if untouched next retro, close it
   as "accepted limitation" per the process-smell rule.
3. **Next milestone: consider a template feature** (e.g. golden tests or
   theming presets) once announcement feedback arrives — owner:
   maintainer via `/plan`, after adoption signals exist.

## Summary

The second cycle shipped a polish release in a single usage-gated loop:
perfect pub score recorded, CI warnings eliminated, and the two worst
first-run CLI failure modes replaced with helpful errors — 4 issues, 4
green PRs, zero failures, and the weekly-limit gate never had to fire.
The project is now technically clean on every axis it controls
(score 160/160, CI 100%, zero lint debt); what it needs next is users.
