# Retro — v0.4.0 "--backend supabase" (compact)

- **Milestone**: v0.4.0 (closed 2026-08-03) — issues #34 #35, PRs #36 #37 #38
- **Release**: [fluframe 0.4.0](https://pub.dev/packages/fluframe) —
  **first fully automated publish** (tag push → gates → OIDC), zero
  human steps.

## Highlights

- ADR 0001 mechanism shipped and immediately proved its design: the
  addon e2e caught supabase_flutter's `anonKey` → `publishableKey`
  deprecation (post-cutoff API change) before it could reach users.
- Second real defect caught pre-merge: `app_test.dart` bypassed the
  helper overrides, so backend variants would have had a red suite —
  fixed with shared `appTestOverrides`.
- Suite: CLI 28 unit + 2 e2e variants; template 43.

## Previous action items

Announce (carry, due 08-10) — still open, now with `--backend supabase`
as the headline. Stage-2 go/no-go — **DONE** (this milestone). Record
pana/downloads — rolling, next `/oss-triage`.

## Action items

1. Announce (final carry — due 08-10, owner: maintainer).
2. `--backend firebase` (v0.5.0) — reuse the mechanism as-is.

## Note

Weekly usage crossed 91% during this milestone; the 96% loop gate
remains armed.
