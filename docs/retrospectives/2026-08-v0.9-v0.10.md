# Retro — v0.9.0 "error hooks" + v0.10.0 "contributor readiness" (compact)

- **Milestones**: v0.9.0 (issue #60, PRs #61 #62) shipped as
  [fluframe 0.9.0](https://pub.dev/packages/fluframe); v0.10.0 is
  repo-only (no CLI change → no release), issues #63–#66 seeded.

## Highlights

- Error handling seam closed the last obvious boilerplate gap — every
  uncaught error now flows through one documented file, wired without
  moving a single addon anchor (variant e2e stayed green untouched).
- The repo is now contributor-shaped: one-page architecture doc with the
  six CI-enforced invariants, a "Where to start" funnel in CONTRIBUTING,
  and four REAL good-first-issues (l10n ja, weather cities, teal preset,
  demo GIF) — each stating files + acceptance criteria.

## Action items

1. **Announce** (owner: maintainer, due 08-10) — now genuinely ready:
   9 releases, 160/160, examples, architecture doc, starter issues.
2. Next `/oss-triage` after announcement: record first external signals.
