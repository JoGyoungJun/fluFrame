# Retro — v0.11 → v0.14 (combined)

- **Milestones**: v0.11 (--error-reporting sentry), v0.12 (analytics
  seam), v0.13 (--analytics amplitude), v0.14 (metadata + upgrade), all
  closed 2026-08-03. Published: 0.11.0; 0.12.0 tagged awaiting the
  publish window; 0.13/0.14 consolidate into 1.0.0 (never published →
  never valid upgrade bases; policy in docs/versioning.md).

## Highlights

- The addon mechanism generalized twice without redesign (stacking,
  patch-only, three axes) — ADR 0001's abstraction held.
- `fluframe upgrade` shipped: the e2e-guarded pub.dev bundles became
  reconstructable BASEs (ADR 0002), and the whole merge engine is
  unit-tested offline against real `git merge-file`.
- The fully-stacked e2e caught two more post-cutoff SDK realities
  (amplitude per-file imports, Future-returning track()).

## Incidents & learnings

- pub.dev publish rate limit (12/day) discovered by hitting it; recovery
  and cadence rules now live in /release.
- usage-gate exit codes break `&&` chains — a protected-main push
  rejection caught the mistake; poll gates separately.

## Action items

1. Publish window (owner: maintainer/Claude, next morning KST):
   `gh run rerun 30788069780` (0.12.0) → bump+tag 1.0.0.
2. Announce 1.0.0 instead of 0.8.0 — strictly better story (owner:
   maintainer, due 08-10).
