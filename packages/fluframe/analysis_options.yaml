include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    # Bundled app template (synced from /template by tool/sync_template.dart);
    # its files reference Flutter dependencies this package does not have.
    - templates/**
