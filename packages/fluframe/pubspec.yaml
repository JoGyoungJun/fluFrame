name: fluframe
description: >-
  Generate production-ready Flutter apps from the fluFrame boilerplate —
  Riverpod 3, go_router, localization, theming, dio, and tests wired
  out of the box.
version: 0.6.0
repository: https://github.com/JoGyoungJun/fluFrame
issue_tracker: https://github.com/JoGyoungJun/fluFrame/issues
topics:
  - flutter
  - cli
  - boilerplate
  - riverpod
  - scaffolding

environment:
  sdk: ^3.12.0

executables:
  fluframe:

dependencies:
  args: ^2.7.0
  io: ^1.0.5
  path: ^1.9.1

dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^10.3.0
