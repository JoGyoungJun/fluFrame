# Synced template bundle — regenerate with: dart run tool/sync_template.dart
templates/

.dart_tool/
build/
# pubspec.lock is ignored by the root .gitignore's packages/*/pubspec.lock
# rule, which carries the policy comment — do not duplicate it here.
