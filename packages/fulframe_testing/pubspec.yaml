name: fulframe_testing
description: Testing helpers and fakes for FulFrame apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  fulframe_core:
    path: ../fulframe_core

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
