name: fulframe_app
description: Flutter-facing app shell and UI helpers for FulFrame apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fulframe_core:
    path: ../fulframe_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
