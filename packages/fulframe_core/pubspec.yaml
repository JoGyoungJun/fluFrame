name: fulframe_core
description: Core contracts and pure Dart primitives for FulFrame apps.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
