name: fulframe_workspace
description: Workspace metadata for the FulFrame open-source Flutter app framework.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  melos: ^6.3.0
